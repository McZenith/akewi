import { ExpoRoot } from 'expo-router';

// This file is not being used directly. The app entry point
// is now handled by Expo Router in the app/ directory.
// We're keeping this file for compatibility with Expo tooling.
export default function App() {
  return null;
}
